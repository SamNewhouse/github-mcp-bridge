# This workflow has been superseded by ci.yml which runs the same
# jobs (typecheck, unit, integration) as parallel jobs on both
# pull_request and push to main. This file is kept to avoid a
# breaking rename but will never trigger automatically.

name: Test (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See ci.yml."
