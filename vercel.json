{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff HEAD~1 --name-only | grep -qvE '^tests/'",
  "rewrites": [
    { "source": "/", "destination": "/api/mcp" },
    { "source": "/health", "destination": "/api/health" }
  ]
}
